"""Hyperbolic PDE portfolio entry point.

This repository snapshot preserves curated numerical outputs (figures and GIFs)
from a computational modelling practice on 1D hyperbolic PDEs.

The original uploaded source files named
`codigocompletopractica5bloqueII.py` / `codigopractica5bloqueII.py`
were not present in this repository snapshot, so no numerical update logic has
been rewritten here to avoid introducing non-original model behavior.

Documented components of the original practice:
- linear 1D advection equation,
- square-pulse initial condition,
- periodic boundary conditions,
- FTCS / centered-difference advection scheme,
- upwind advection scheme,
- downwind advection scheme,
- Lax-Friedrichs advection scheme,
- CFL-based timestep selection,
- GIF animation of explicit advection methods,
- inviscid Burgers equation,
- sinusoidal Burgers initial condition,
- upwind Burgers update,
- centered Burgers update,
- periodic boundary treatment for Burgers,
- Burgers GIF animations,
- classic-versus-matrix Burgers comparison,
- L2 error calculation,
- error versus timestep study,
- runtime versus timestep study,
- artificial diffusion analysis,
- Dirichlet, Neumann and periodic boundary-condition comparison.
"""

if __name__ == "__main__":
    print(
        "This portfolio repository preserves figures and documentation for the "
        "hyperbolic PDE practice."
    )
